# CI Workflow: Lint + Test
# TODO: Implement full workflow based on MASTER_SPEC.md Section 5.6
#
# Required jobs:
# - lint: ruff check, mypy
# - test: pytest with coverage
# - Matrix: Python 3.11, 3.12
#
# Triggers: push to main, pull_request

name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # TODO: Add ruff and mypy steps

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # TODO: Add pytest with coverage
